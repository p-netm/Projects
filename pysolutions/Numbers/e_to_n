"""Just like the previous problem, but with e instead of PI. Enter a number and have the program generate e up to that
many decimal places. Keep a limit to how far the program will go"""
from pi_to_n import put_precision, math

if __name__ == "__main__":
    put_precision(math.e)
